import { describe, it } from "vitest";
import { CATEGORIES_SLUG_TO_EXCLUDE_FROM_LBC } from "./constants";

describe('CATEGORIES_SLUG_TO_EXCLUDE_FROM_LBC', () => {
  it('should be an array', () => {
    console.log(CATEGORIES_SLUG_TO_EXCLUDE_FROM_LBC)
  })
})